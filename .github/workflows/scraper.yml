name: Purse Forum Scraper

on:
  workflow_dispatch:
    inputs:
      keywords:
        description: '가격 키워드 (쉼표로 구분: price,cost,paid,spent,$,₩,won,krw)'
        required: true
        default: 'price,cost,paid,spent,total,$,₩,won,krw,usd,dollar'
      start_page:
        description: '시작 페이지'
        required: false
        default: '1'
      max_pages:
        description: '최대 페이지 수'
        required: false
        default: '3'
  
  schedule:
    - cron: '0 0 * * *'
  
  push:
    branches: [ main ]

jobs:
  scrape:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 코드 체크아웃
      uses: actions/checkout@v3
    
    - name: 🐍 Python 설정
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: 🌐 Chrome 설치
      uses: browser-actions/setup-chrome@latest
      with:
        chrome-version: stable
    
    - name: 🚗 ChromeDriver 설치
      uses: nanasess/setup-chromedriver@master
    
    - name: 📦 패키지 설치
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: 🔑 구글 인증 설정
      run: |
        echo '${{ secrets.GOOGLE_CREDENTIALS }}' > credentials.json
    
    - name: ⚙️ 설정 파일 업데이트
      run: |
        python -c "
        import re
        
        with open('config.py', 'r', encoding='utf-8') as f:
            content = f.read()
        
        content = content.replace('YOUR_SPREADSHEET_ID_HERE', '${{ secrets.SPREADSHEET_ID }}')
        
        if '${{ github.event_name }}' == 'workflow_dispatch':
            keywords = '${{ github.event.inputs.keywords }}'.replace('\"', '\\\"')
            start_page = '${{ github.event.inputs.start_page }}'
            max_pages = '${{ github.event.inputs.max_pages }}'
            
            content = re.sub(r'PRICE_KEYWORDS = \".*?\"', f'PRICE_KEYWORDS = \"{keywords}\"', content)
            content = re.sub(r'START_PAGE = \d+', f'START_PAGE = {start_page}', content)
            content = re.sub(r'MAX_PAGES = \d+', f'MAX_PAGES = {max_pages}', content)
        
        with open('config.py', 'w', encoding='utf-8') as f:
            f.write(content)
        
        print('✅ config.py 업데이트 완료!')
        "
    
    - name: 🚀 크롤러 실행
      run: |
        python scraper.py
    
    - name: 📋 로그 업로드
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: scraper-logs
        path: |
          *.log
          screenshots/
